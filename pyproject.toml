[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "execution-aware-data-curation"
version = "0.1.0"
description = "Execution-aware data curation and provenance for generated-code artifacts."
requires-python = ">=3.8"
authors = [{name = "Jingxin Wang"}]
